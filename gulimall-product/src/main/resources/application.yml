spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://49.235.76.203:3306/gulimall_pms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      # 配置主键自增
      id-type: auto

server:
  port: 10000
